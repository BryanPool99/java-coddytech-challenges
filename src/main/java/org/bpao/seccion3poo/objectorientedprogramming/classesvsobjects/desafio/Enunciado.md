Crea una clase Car (el plano) y el programa creará dos objetos (instancias) diferentes a partir de ella:

- Campos: brand (String) y year (int)
- Un constructor que establece ambos campos
- Un método getInfo() que devuelve ```"<brand> (<year>)"```

