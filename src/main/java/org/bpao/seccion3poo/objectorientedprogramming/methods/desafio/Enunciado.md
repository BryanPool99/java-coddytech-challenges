# Desafío Intermedio

Crea cuatro métodos en la clase StringHelper:

- toUpperCase(): returns el texto en mayúsculas
- getLength(): returns la longitud del texto
- contains(String word): returns true si el texto contains la palabra
- repeat(int times): returns el texto repeated con espacios

