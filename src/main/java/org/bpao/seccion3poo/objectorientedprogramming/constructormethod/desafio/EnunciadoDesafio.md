# Desafio intermedio

Crea una clase Book con un constructor que inicialice todos los campos:

- Campos private: title, author (String), pages (int)
- Un constructor que takes los tres valores y los asigna usando this
- Métodos getter para cada campo
- Un método getSummary() que devuelva "<title> by <author> (<pages> pages)"