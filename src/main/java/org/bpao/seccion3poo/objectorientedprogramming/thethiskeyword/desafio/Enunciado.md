# Desafio Intermedio
Completa la clase Person utilizando la palabra clave this para distinguir entre campos y parámetros:

Un constructor que utiliza this para asignar parámetros a los campos
Métodos getter que utilizan this para acceder a los campos
Un método getDescription() que devuelve "<name>, age <age>, from <city>"